#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>

int main(int argc, char *argv[])
{

    printf("---- RING! ----\n");
    //printf("\tChild Processes ID %i\n", argv[0]); 
   

   /*
    int id = fork();
    printf("this id is %i\n", id); 

    printf("Hello World\n");
    printf("PID %i", getpid()); 

    //printf("Hello Wrold from id: %d\n", id);
    
    Forking is forking the executing line. 
    Child processes gets born. Starts executing the following lines as well. 
    When child processes has executed the 
    Id = 0, means we are in the child processes
    
    if ( id == 0){
        printf("Hello from child processes\n"); 
    }
    else {
    printf("Hello from the main processes\n");
    }
    */
    return 0;
}